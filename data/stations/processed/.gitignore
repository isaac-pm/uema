# Derived data is regenerated by re-running the notebook that produced it and
# is never committed, following the same convention as `reports/*/.gitignore`.
# Everything here is reproducible output; only this file is tracked.
*
!.gitignore
