# Per-station raw correlation/CCF output — bulky, grows with every new
# station, and fully regenerated by re-running 01_variable_correlation.ipynb.
# Cross-station summary CSVs (ccf_peak_summary.csv,
# conditioned_correlation_summary.csv, primary_correlation_summary.csv,
# station_scope.csv, post_fill_missingness.csv) stay tracked since the
# README quotes exact numbers from them.
*_pearson.csv
*_spearman.csv
*_ccf_*.csv
